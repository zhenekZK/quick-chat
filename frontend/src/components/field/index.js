import styled from 'styled-components';
import { TextField } from '@material-ui/core';

const Field = styled(TextField)`
  border-radius: 15px;
  background: rgba(241, 95, 121, 0.7);
`;

export { Field };
