{
  "name": "quick-chat-backend",
  "version": "1.0.0",
  "description": "Server for quick-chat application",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "server-dependencies": "npm install",
    "client-dependencies": "npm install --prefix ../frontend",
    "dev": "npm run start & npm run start --prefix ../frontend",
    "prepare-env": "npm run server-dependencies && npm run client-dependencies && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "socket.io": "^2.4.0",
    "uuid": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
